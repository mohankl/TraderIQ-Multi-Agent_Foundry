{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy this to settings.local.json and edit. Local-only — not committed. Merged on top of settings.json.",
  "permissions": {
    "allow": [
      "Bash(open:*)"
    ]
  },
  "env": {
    "EDITOR_OF_CHOICE": "code"
  }
}
