# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit
.env
.env.*

# macOS
.DS_Store

# Playwright MCP test artifacts
.playwright-mcp/

# Claude Code per-user overrides (template is .claude/settings.local.json.example)
.claude/settings.local.json

# Claude Code runtime state (scheduled wakeup lockfile)
.claude/scheduled_tasks.lock
